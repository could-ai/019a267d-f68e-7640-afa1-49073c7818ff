import 'package:couldai_user_app/app/modules/auth/login/login_controller.dart';

class LoginBinding {
  void dependencies() {
    // For now, we don't have any dependencies to inject.
    // But this is a good place to register services or controllers.
    // For example:
    // Get.lazyPut<LoginController>(() => LoginController());
  }
}
