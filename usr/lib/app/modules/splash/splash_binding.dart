import 'package:couldai_user_app/app/modules/splash/splash_controller.dart';

class SplashBinding {
  void dependencies() {
    // For now, we don't have any dependencies to inject.
    // But this is a good place to register services or controllers.
    // For example:
    // Get.lazyPut<SplashController>(() => SplashController());
  }
}
